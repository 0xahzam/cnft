import { Keypair } from "@solana/web3.js";
import bs58 from "bs58";

export const wallet = [
  // ..
];

// const k = Keypair.fromSecretKey(new Uint8Array(wallet));
// console.log(k.secretKey.toString());

// const phrase = bs58.encode(k.secretKey);
// console.log(phrase);
